# 安全策略

## 支持范围

安全修复优先覆盖默认分支的最新版本。仓库目前没有长期支持分支或已发布版本承诺。

## 报告漏洞

请不要通过公开 Issue 报告漏洞，也不要提交真实 token、私有航线或可识别设备数据。

优先使用 GitHub 仓库的 **Security > Report a vulnerability** 私密报告功能。若该功能不可用，请在仓库建立一个不包含漏洞细节的普通 Issue，请求维护者提供私密联系方式。

报告应包含：

- 受影响的 commit 或版本；
- 问题类型、影响和所需前置条件；
- 最小复现步骤或概念验证；
- 建议修复（如有）；
- 是否已向其他方披露。

维护者会尽力在 7 天内确认收到报告，并在完成评估后协调修复和披露时间。该时间是目标，不是服务等级承诺。

## 部署注意事项

默认项目是开发和演示配置，不是面向互联网的安全基线：

- API 没有身份认证或授权，不应直接暴露到不可信网络。
- 生产环境必须设置精确的 `ALLOWED_ORIGINS`，并由反向代理终止 TLS。
- `VITE_` 变量会进入浏览器包，不能包含秘密。
- 外部任务文件应由可信部署流程提供，并保持只读。
- 接入真实遥测源前必须增加设备认证、消息完整性、审计、限流和网络隔离。
- 本项目不提供真实航空器控制、避障或适航安全保证。

依赖更新由 Dependabot 辅助发现，但部署者仍需根据自身环境执行漏洞扫描和风险评估。
